<div{{ attributes }}>
    <button
        type="button"
        class="btn btn-primary"
        {{ live_action('initPoint') }}
    >Init point</button>

    <hr>

    <h2>Point (<code>print_r</code>)</h2>
    <output>{{ print_r(point) }}</output>
</div>
