# CHANGELOG

## 3.6.0

- Add the component
- Add the `ux_image()` Twig function
- Add `config/routes/glide.php`, a public route file the `symfony/ux-glide-image` bridge's README documents importing from your application
