# Contributing

Install the test app:

    $ composer install
    $ cd Tests/app
    $ yarn install
    $ yarn build

Start the test app:

    $ symfony serve

## Run tests

    $ php vendor/bin/simple-phpunit
