{# @prop id string Unique identifier. #}
{# @prop variant 'default'|'line' The visual style variant. #}
{# @prop open boolean Whether it is open. #}
{%- props id, variant = 'default', open = false -%}
<div></div>
