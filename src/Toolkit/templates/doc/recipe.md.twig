{#
    Renders a recipe's documentation as Markdown. Relocated from ux.symfony.com's
    `_base_component.md.twig`, de-coupled from the site: examples use the portable `::: example`
    directive, and `format` ('html' | 'markdown') replaces the old `is_llm` flag.
#}
{%- if format == 'markdown' -%}
# {{ recipe.manifest.name }} ({{ kit.manifest.name }} kit)

{% endif -%}
{{ recipe.manifest.description|trim }}

::: example Demo

## Installation
{% if format == 'markdown' %}
```shell
php bin/console ux:install {{ recipe.name }} --kit {{ kit_id }}
```
{% else %}
::: tabs

:: tab Automatic

```shell
php bin/console ux:install {{ recipe.name }} --kit {{ kit_id }}
```

:: tab Manual
{% if php_package_dependencies is not empty %}
Install the following Composer dependencies:

```shell
composer require {{ php_package_dependencies|join(' ') }}
```
{% endif %}
{% if npm_package_dependencies is not empty %}
Install the following front-end dependencies:

```shell
npm install {{ npm_package_dependencies|join(' ') }}
# ...or with Symfony AssetMapper
php bin/console importmap:require {{ importmap_package_dependencies|join(' ') }}
```
{% endif %}
Copy the following file(s) into your app:
{% for file in files %}
```{{ file.language }}
{{ file.content|trim }}
```
{% endfor %}
:::
{% endif %}
{% if recipe.doc is not null %}

{{ recipe.doc|trim }}
{% endif %}
{% if api_reference is not empty %}

## API Reference
{% for component_name, ref in api_reference %}
### `{{ '<' }}twig:{{ component_name }}{{ '>' }}`
{% if ref.props is not empty %}
| Prop | Type | Default |
|:-----|:-----|:--------|
{% for prop in ref.props %}
| `{{ prop.name }}` | `{{ prop.type }}` | {{ prop.default is not null ? '`' ~ prop.default ~ '`' : '-' }} |
{% endfor %}
{% endif %}
{% if ref.blocks is not empty %}

| Block | Description |
|:------|:------------|
{% for block in ref.blocks %}
| `{{ block.name }}` | {{ block.description }} |
{% endfor %}
{% endif %}
{% endfor %}
{% endif %}
