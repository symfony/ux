<input
    type="range"
    class="{{ ('w-full accent-primary cursor-pointer disabled:cursor-not-allowed disabled:opacity-50 focus-visible:outline-none focus-visible:ring-[3px] focus-visible:ring-ring/50 rounded-full ' ~ attributes.render('class'))|tailwind_merge }}"
    data-slot="slider"
    {{ attributes }}
>
