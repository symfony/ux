{# @block content The inline code content. #}
<code
    data-slot="typography-inline-code"
    {{ attributes.defaults({
        class: 'relative rounded bg-muted px-[0.3rem] py-[0.2rem] font-mono text-sm font-semibold'|tailwind_classes,
    }) }}
>
    {%- block content %}{% endblock -%}
</code>
