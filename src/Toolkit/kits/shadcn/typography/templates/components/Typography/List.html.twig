{# @block content The list items (`<li>`). #}
<ul
    data-slot="typography-list"
    {{ attributes.defaults({
        class: 'my-6 ml-6 list-disc [&>li]:mt-2'|tailwind_classes,
    }) }}
>
    {%- block content %}{% endblock -%}
</ul>
