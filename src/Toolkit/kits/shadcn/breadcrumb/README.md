# Breadcrumb

Displays the path to the current resource using a hierarchy of links.

::: example Demo {"height": "150px"}

## Installation

::: installation

## Usage

::: example Usage

## Examples

### Basic

A basic breadcrumb with a home link and a components link.

::: example Basic {"height": "150px"}

### Custom separator

Use a custom component as `children` for `Breadcrumb:Separator` to create a custom separator.

::: example Custom Separator {"height": "150px"}

### Collapsed

We provide a `Breadcrumb:Ellipsis` component to show a collapsed state when the breadcrumb is too long.

::: example Collapsed {"height": "150px"}

### Link component

To use a custom link component from your routing library, you can pass the `href` attribute to `Breadcrumb:Link`.

::: example Link component {"height": "150px"}

### RTL

To enable RTL support, set the `dir="rtl"` attribute on the root element.

::: example RTL

## API Reference

::: api-reference
