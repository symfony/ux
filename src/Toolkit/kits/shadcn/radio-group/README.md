# Radio Group

A set of checkable buttons—known as radio buttons—where no more than one of the buttons can be checked at a time.

::: example Demo {"height": "200px"}

## Installation

::: installation

## Usage

::: example Usage

## Examples

### Description

Radio group items with a description using the `Field` component.

::: example Description {"height": "300px"}

### Choice Card

Use `Field:Label` to wrap the entire `Field` for a clickable card-style selection.

::: example Choice Card {"height": "350px"}

### Fieldset

Use `Field:Set` and `Field:Legend` to group radio items with a label and description.

::: example Fieldset {"height": "250px"}

### Disabled

Use the `disabled` prop on `RadioGroup:Item` to disable individual items.

::: example Disabled

### Invalid

Use `aria-invalid` on `RadioGroup:Item` and `data-invalid` on `Field` to show validation errors.

::: example Invalid {"height": "300px"}

### RTL

To enable RTL support, set the `dir="rtl"` attribute on the root element.

::: example RTL {"height": "450px"}

## API Reference

::: api-reference
