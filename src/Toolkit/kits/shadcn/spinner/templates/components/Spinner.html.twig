{%- props label = 'Loading' -%}
<twig:ux:icon
    name="lucide:loader-2"
    class="{{ ('size-4 animate-spin ' ~ attributes.render('class'))|tailwind_merge }}"
    {{ ...attributes.defaults({
        'data-slot': 'spinner',
        role: 'status',
        'aria-label': label,
    }) }}
/>
