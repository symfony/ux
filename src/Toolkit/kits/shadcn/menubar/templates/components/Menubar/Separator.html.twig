<div
    role="separator"
    class="{{ ('-mx-1 my-1 h-px bg-muted ' ~ attributes.render('class'))|tailwind_merge }}"
    {{ attributes.defaults({
        'data-slot': 'menubar-separator',
    }) }}
></div>