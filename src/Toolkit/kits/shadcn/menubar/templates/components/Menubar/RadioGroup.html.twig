{%- props
    ## string|null The radio group form field name, shared by all `Menubar:RadioItem` children that omit their own.
    name = null
-%}
{%- do provide('menubar.radioGroupName', name) -%}
<div
    role="group"
    data-slot="menubar-radio-group"
    {{ attributes }}
>
    {##- One or more `Menubar:RadioItem` components. -#}
    {%- block content %}{% endblock -%}
</div>
