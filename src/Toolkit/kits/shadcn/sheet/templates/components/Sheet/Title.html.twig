{# @block content The sheet title #}
<h2
    class="{{ ('text-lg font-semibold leading-none tracking-tight ' ~ attributes.render('class'))|tailwind_merge }}"
    {{ attributes.defaults({
        'data-slot': 'sheet-title',
    }) }}
>
    {%- block content %}{% endblock -%}
</h2>