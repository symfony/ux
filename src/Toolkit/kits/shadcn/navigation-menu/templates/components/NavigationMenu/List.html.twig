<ul
    data-slot="navigation-menu-list"
    {{ attributes.defaults({
        class: 'flex flex-1 list-none items-center justify-center gap-1'|tailwind_classes,
    }) }}
>
    {##- The `NavigationMenu:Item` children. -#}
    {%- block content %}{% endblock -%}
</ul>
