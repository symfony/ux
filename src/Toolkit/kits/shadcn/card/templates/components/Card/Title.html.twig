{# @block content The title text of the card. #}
<div
    data-slot="card-title"
    {{ attributes.defaults({
        class: 'cn-font-heading text-base leading-snug font-medium group-data-[size=sm]/card:text-sm'|tailwind_classes,
    }) }}
>
    {%- block content %}{% endblock -%}
</div>
