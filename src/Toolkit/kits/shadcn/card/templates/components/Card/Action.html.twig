<div
    data-slot="card-action"
    {{ attributes.defaults({
        class: 'col-start-2 row-span-2 row-start-1 self-start justify-self-end'|tailwind_classes,
    }) }}
>
    {##- The action area placed in the top-right corner of the card header. -#}
    {%- block content %}{% endblock -%}
</div>
