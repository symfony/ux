<div
    data-slot="item-content"
    {{ attributes.defaults({
        class: 'flex flex-1 flex-col gap-1 group-data-[size=xs]/item:gap-0 [&+[data-slot=item-content]]:flex-none'|tailwind_classes,
    }) }}
>
    {##- The main content area, typically includes `Item:Title` and `Item:Description`. -#}
    {%- block content %}{% endblock -%}
</div>
