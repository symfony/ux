# Item

A versatile component for displaying content with media, title, description, and actions.

::: example Demo {"height": "250px"}

## Installation

::: installation

## Usage

::: example Usage

## Examples

### Variant

Use the `variant` prop to change the visual style of the item.

::: example Variants {"height": "400px"}

### Size

Use the `size` prop to change the size of the item. Available sizes are `default`, `sm`, and `xs`.

::: example Size {"height": "400px"}

### Icon

Use `Item:Media` with `variant="icon"` to display an icon.

::: example Icon

### Avatar

Use `Item:Media` to display an avatar.

::: example Avatar {"height": "300px"}

### Image

Use `Item:Media` with `variant="image"` to display an image.

::: example Image {"height": "350px"}

### Group

Use `Item:Group` to group related items together.

::: example Group {"height": "350px"}

### Header

Use `Item:Header` to add a header above the item content.

::: example Header {"height": "400px"}

### Link

Use the `as` prop to render the item as a link. The hover and focus states will be applied to the anchor element.

::: example Link {"height": "300px"}

### RTL

To enable RTL support, set the `dir="rtl"` attribute on the root element.

::: example RTL {"height": "400px"}

## API Reference

::: api-reference
