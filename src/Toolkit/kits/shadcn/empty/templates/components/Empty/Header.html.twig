<div
    data-slot="empty-header"
    {{ attributes.defaults({
        class: 'flex max-w-sm flex-col items-center gap-2'|tailwind_classes,
    }) }}
>
    {##- The header area, typically contains `Empty:Title` and `Empty:Description`. -#}
    {%- block content %}{% endblock -%}
</div>
