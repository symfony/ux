{# @block content The main content area, typically contains actions like buttons. #}
<div
    data-slot="empty-content"
    {{ attributes.defaults({
        class: 'flex w-full max-w-sm min-w-0 flex-col items-center gap-2.5 text-sm text-balance'|tailwind_classes,
    }) }}
>
    {%- block content %}{% endblock -%}
</div>
