{# @block content The grouped avatars, typically multiple `Avatar` components. #}
<div
    {{ attributes.defaults({
        class: 'group/avatar-group flex -space-x-4 rtl:space-x-reverse *:border-2 *:border-buffer'|tailwind_classes,
    }) }}
>
    {%- block content %}{% endblock -%}
</div>
