{# @block content The table footer rows, typically `Table:Row` components. #}
<tfoot
    {{ attributes.defaults({
        class: 'font-semibold text-heading'|tailwind_classes,
    }) }}
>
    {%- block content %}{% endblock -%}
</tfoot>
