{% set default_attributes = {
    'class': 'Icon Icon--' ~ name,
    'role': 'img',
    'aria-hidden': 'true',
    'aria-label': label,
} %}
<i {{ attributes.defaults(default_attributes) }}>
    {{- rawSvg|raw -}}
</i>
