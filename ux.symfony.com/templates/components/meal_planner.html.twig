{# See src/Form/MealPlannerForm.php for the form magic #}
<div
    {{ attributes }}
>
    {{ form_start(form) }}
        {{ form_row(form.meal) }}
        {{ form_row(form.mainFood) }}
    {{ form_end(form) }}
</div>
